enum CommitCount {
  Zero = "Zero",
  Few = "Few",
  Some = "Some",
  Many = "Many",
  ALot = "ALot",
}

enum Font {
  SubwayTracker = "SubwayTracker",
}

const SUNDAY = 0;

export { CommitCount, Font, SUNDAY };
